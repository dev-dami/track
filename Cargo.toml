[package]
name = "track"
version = "0.1.0"
edition = "2021"

[dependencies]
logos = "0.14"
inkwell = { version = "0.9.0", features = ["llvm22-1"] }
toml = "0.8"
serde = { version = "1", features = ["derive"] }
